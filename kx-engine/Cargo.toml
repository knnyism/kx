[package]
name = "kx-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true

ash = { version = "0.38", features = ["linked"] }
ash-bootstrap = "0.1"
gpu-allocator = "0.28"
raw-window-handle = "0.6"

[build-dependencies]
hassle-rs = "0.12"
