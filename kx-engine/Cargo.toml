[package]
name = "kx-engine"
version = "0.1.0"
edition = "2024"

[dependencies]
anyhow.workspace = true

ash = { version = "0.38.0", features = ["linked"] }
ash-window = "0.13.0"
gpu-allocator = "0.28.0"
raw-window-handle = "0.6"
